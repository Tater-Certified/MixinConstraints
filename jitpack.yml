jdk:
  - openjdk21
before_install:
   - sdk install java 21.0.7-open
   - sdk use java 21.0.7-open
   - sdk install java 17.0.9-open
install:
   - echo "Running a custom install command"
   - ./gradlew build publishToMavenLocal -Pgroup=com.github.Tater-Certified -Pversion=GIT_COMMIT
